import 'package:flutter/material.dart';

class AppColor {
  // Background gradient bases
  static const Color primary = Color(0xFF6A007F); // Rich purple-magenta
  static const Color secondary = Color(0xFF0D001A); // Deep dark purple

  // Text colors
  static const Color primaryText = Color(0xFFFFFFFF); // White
  static const Color secondaryText = Color(0xFFAFA3B8); // Soft lavender-gray

  // Border + stroke colors
  static const Color textFieldBorder = Color(0xFF5B3F61); // Muted purple-gray

  // Buttons
  static const Color primaryButton = Color(0xFFCF2BAD); // Magenta highlight
  static const Color enabledButton = Color(0xFF30C267); // Good green (success)
  static const Color disabledButton = Color(0xFF3A2A3F); // Deep muted plum

  // Highlights (for glow, focus, icons)
  static const Color highlightColor = Color(0xFFE65ACF); // Lighter magenta glow

  // Screen specific
  static const Color inputFill = Color(0xFF1E1E1E);
  static const Color primaryPink = Color(0xFFFF2D55);
}
